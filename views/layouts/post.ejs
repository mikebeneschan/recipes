<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recipes</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <!-- Header -->
  <%- include('../partials/header') %>

  <!-- Main Page Content -->
  <main>
    <h1 style="margin-bottom: 4px"><%= data.title %></h1>
    <p class="st" style="margin: 2px; color: #81b29a; font-style: italic"><%=data.subtitle%></p>
    <img src = "/images/<%= data.image %>" aspect-ratio=1 height="400px" style="margin-top: 34px">
    <p><%= data.content %></p>
  </main>

  <!-- Footer -->
  <!-- <%- include('../partials/footer') %> -->
</body>
</html>